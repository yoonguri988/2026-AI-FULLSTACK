<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.the703.dao.BoardMapper">
  <select id="selectAll" resultType="BoardDto">
  select * from mvcboard2 order by bno desc
  </select>
  <select id="select" parameterType="int" resultType="BoardDto">
  select * from mvcboard2 where bno = #{bno}
  </select>
  <insert id="insert" parameterType="BoardDto">
  insert into mvcboard2 (bname, bpass,btitle, bcontent, bip) 
         values (#{bname}, #{bpass}, #{btitle}, #{bcontent}, #{bip})
  </insert>
  <update id="update" parameterType="BoardDto">
  update mvcboard2 set btitle=#{btitle}, bcontent=#{bcontent}, bip=#{bip} where bno = #{bno}
  </update>
  <delete id="delete" parameterType="int">
  delete from mvcboard2 where bno = #{bno}
  </delete>
</mapper>