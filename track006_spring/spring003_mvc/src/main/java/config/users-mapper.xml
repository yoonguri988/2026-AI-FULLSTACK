<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.the703.dao.UserInfoMapper">
  <select id="selectAll" resultType="UserInfoDto">
  	select * from users order by uno desc;
  </select>
  <select id="select" resultType="UserInfoDto" parameterType="int">
  	select * from users where uno= #{uno}
  </select>
  <insert id="insert" parameterType="UserInfoDto">
  	insert into users (nickname, bpass, email, moblie, bip)
  	       values (#{nickname}, #{bpass}, #{email}, #{moblie}, #{bip})
  </insert>
  <update id="update" parameterType="UserInfoDto">
  	update users set nickname=#{nickname}, moblie=#{moblie}, bip=#{bip}
  	where uno = #{uno}
  </update>
  <delete id="delete" parameterType="int">
  	delete from users where uno= #{uno}
  </delete>
</mapper>